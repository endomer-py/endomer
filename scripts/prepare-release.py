"""Legacy entry point: release scripts are maintained directly."""
raise SystemExit("Use scripts/release.ps1 from the ENDOM workspace. This generator no longer overwrites scripts from another survey package.")
